# 周期重置修复与在线容量核对（2026-09-24）

本报告只使用本地快照副本 `/tmp/cqe-repair-working.sqlite`。原始快照 `/tmp/cqe-calibration-20260923.sqlite` 只读，SHA-256 为 `e4d226d74b51eb887ee8f0962dfa1b1cceb789d7b0a1111ac835768250ee77df`。未访问线上数据库或发送模型请求。

## 重置漏识别

2026-08-28 00:26:44（北京时间）首个新读数从 29% 降到 0%，`reset_at` 从 09-01 22:14 前移至新的 09-04 00:27 窗口；之后相同窗口的 0% 读数持续超过 60 秒。旧版 `cycles.go` 的提前重置条件要求 `!resetAtChanged`，因此必然挡住这次变化；计划重置分支也不成立，因为旧窗口要到 09-01 才截止，而新窗口在 08-28 已开始。旧版随后原地更新 `quota_cycles.reset_at`，丢失旧窗口峰值基线。2026-09-12 的代码已引入 `advancedEarlyResetObservation`，目前分支能够识别该事件；这次补入脱敏原始时序测试，已在事发时版本 `7e5eff2` 上确认失败，在当前代码上通过。另在 `cycles.go` 标明不能重新加回 `!resetAtChanged` 限制。

历史修复以“新窗口开始时间接近首次低读数、至少 3 个连续低读数持续 60 秒、合并周期在该边界后仍持续至少 1 小时”为条件。最后一条排除了其他周期结束前一两分钟的短暂低读数。修复事务内拆周期、重归属 `usage_events.cycle_id` 与 `quota_samples`、补 0% 首样本、重算样本累计值和周期摘要，写入 `metadata.missed_advanced_early_resets_v1=complete`；重复运行没有新增周期或样本。相关 `quota_segments` 重建，旧权重拟合失效待重新拟合。

| 本地快照 | 周期数 | 周期 4 | 新周期 | 其余周期 |
|---|---:|---|---|---|
| 修复前 | 11 | 08-25 22:17～08-30 05:24；峰值 52%；末态 `reset_at` 09-06 05:24；15,455 请求、673 样本 | 无 | 1–3、5–11 |
| 修复后 | 12 | 08-25 22:17～08-28 00:26；峰值 29%；`reset_at` 09-01 22:14；5,962 请求、382 样本 | ID 12，08-28 00:26～08-30 05:24；峰值 52%；`reset_at` 09-04 00:27；9,493 请求、292 样本（含补建 0% 样本） | 身份、边界和峰值不变 |

在副本上统一重算 Credits 口径后，两个拆分周期的样本首点到峰值的平均每 1% 计价值分别为 **511** 和 **518 Credits**。修复前合并统计约 808 Credits/1%，是跨重置混算的产物，不是稳定的额度容量。

## 周期前 10% 容量误差

将副本历史请求统一重算为 Credits。每个周期取首次跨越 1%～10% 的点，仅用该时点以前的跨越段在线更新；真值使用该周期首样本到峰值样本的 Credits 增量除以百分点增量。数字是十个早期预测点的平均绝对**相对**误差，单位为百分比；这不是未来容量已知时的在线指标，而是事后评分。

| 周期 | 最终每 1% Credits | 旧 `estimateCapacity` | 周期尺度在线估计 |
|---|---:|---:|---:|
| 2 | 525 | 无可用估计 | 2.4% |
| 3 | 574 | 10.9% | 4.0% |
| 4（拆分前半） | 511 | 16.2% | 17.5% |
| 5 | 545 | 11.7% | 8.6% |
| 6 | 502 | 10.1% | 4.9% |
| 7 | 384 | 10.4% | 13.2% |
| 8 | 378 | 11.3% | 4.8% |
| 9 | 357 | 7.2% | 3.1% |
| 10 | 368 | 12.1% | 2.9% |
| 11 | 367 | 13.3% | 2.9% |
| **共同可比周期均值（3–11）** | — | **11.5%** | **6.9%** |

周期 1 从 11% 才开始采样，不能评价前 10%；新周期 12 的首个补建样本为 0%，随后直至 29% 没有采样跨越点，也不能评分。周期 4 和 7 在前 10% 上在线尺度反而较差，故优势是总体而非逐周期保证。新周期零跨越时继承上周期尺度，并在 log 空间增加 σ=0.35 的随机游走不确定性；随后用本周期跨越段更新。当前周期容量、剩余计价值、按模型剩余 Token 和燃尽时间使用同一计价值尺度。

本地快照最近 7 天的模型分摊合计 121.76%，账号实测额度增长 124.00%，差 **-2.24 个百分点**；最近 30 天为 585.70% 对 593.00%，差 **-7.30 个百分点**。这只检验总量接近，不能验证每个模型的真实上游扣减；失败流和账号外部流量仍可能造成偏差。

## 默认口径与局限

新安装默认 Credits；已有保存的 `legacy_api`/`current_api` 设置照旧，仪表盘提供可关闭的一键切换建议。提示所引历史滚动回测 MAE 为 Credits 0.277、legacy API 0.349。Credits 口径的 Astra 使用公开比例，不再乘 1.8；API 口径仍可手动校准，learned 使用其拟合倍率。Astra 倍率若仍锁定先验，仪表盘提示同一周期分别纯用 Sol 和 Astra 各约 5% 额度以增加可辨识度。

区间来自跨越段噪声及周期间随机游走先验，未覆盖账户外用量、整数化百分比、失败请求漏记或未来额度政策变化。早期 Token 容量还依赖最近请求的 Token/计价值比例，模型组合变化会改变这个换算。
